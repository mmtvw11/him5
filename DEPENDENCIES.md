# dependencies дополнительно добавить в pubspec.yaml:

# Функциональное программирование (Either для обработки ошибок)
from pub.dev:
  dartz: ^0.10.1

# Если нужна работа с JSON:
# json_serializable: ^6.7.0
# json_annotation: ^4.8.1

dev_dependencies:
  # Для генерации кода JSON
  # build_runner: ^2.4.6
